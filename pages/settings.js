import Head from 'next/head';

import Settings from '../src/components/Settings';

const SettingsPage = () => (
	<>
		<Head>
			<title>Backloggr - Settings</title>
		</Head>
		<Settings />
	</>
);

export default SettingsPage;
